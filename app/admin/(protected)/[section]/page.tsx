import Link from "next/link";
export default async function AdminSection({ params }: { params: Promise<{ section: string }> }) { const { section } = await params; const title = section.replace(/-/g, " "); return <main className="page-shell py-12"><Link href="/admin" className="text-sm font-semibold text-cobalt">← Dashboard</Link><h1 className="display mt-6 text-5xl capitalize">{title}</h1><div className="mt-8 rounded-2xl bg-chalk p-8"><p className="font-semibold">Connect this view to Supabase.</p><p className="mt-2 max-w-xl text-sm leading-relaxed text-ink/70">The database schema and RLS policies in <code>supabase/schema.sql</code> define the operational records for this section. This protected route is ready for authenticated admin/operations users.</p></div></main>; }
